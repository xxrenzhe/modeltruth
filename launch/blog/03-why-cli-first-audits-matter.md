# Why CLI-First Audits Matter

Many developers prefer to test sensitive API providers locally before trusting a hosted service. The ModelTruth CLI defaults to local execution and requires explicit consent before uploading redacted evidence.

That workflow keeps trust aligned with user control: local-first audits for exploration, hosted monitoring for recurring production checks.
